Multiple NPCs can share the same bot; the bot switches between them.

Bots are registered in config.yml
Commands are named in commands.yml and must have a corresponding class in Command (found by reflection)