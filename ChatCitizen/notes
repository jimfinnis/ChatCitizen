Compiling: you have to play sillybuggers to get it to let you use JSObject.
I did this by adding the jfxrt.jar separately to the build path as an external JAR,
then making it the first JAR scanned in the Order tab.

ChatterBean reloads everything for every instance - use a single instance and make the AliceBot
switch contexts. 